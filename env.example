# AGI System Environment Configuration
DISABLE_NEWS_POLLER=true
ALLOW_REQUESTS=true

MONITOR_LLM_WORKER_TIMEOUT_SECONDS=120

# For manual/development setup
export EXECUTION_METHOD=docker
ENABLE_ARM64_TOOLS=false  # Disable ARM64-specific tools on x86


RPI_HOST=192.168.1.63
DRONE_TOKEN=JnFN2PMNrjlb0r85slGbNWfYFGxD68wd
DRONE_REPO=stevef/agi-runtime
WEAVIATE_URL=http://localhost:8080 

MONITOR_STATIC_DIR=/home/stevef/dev/agi/monitor/static
#DISABLE_NEWS_POLLER=true
# FSM Configuration
FSM_AGENT_ID=agent_1
FSM_AUTONOMY=false
FSM_AUTONOMY_EVERY=60

# Service URLs
HDN_URL=http://localhost:8081
PRINCIPLES_URL=http://localhost:8084
NATS_URL=nats://127.0.0.1:4222
export REDIS_URL=redis://localhost:6379
GOAL_MANAGER_URL=http://localhost:8090

# Database URLs
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=test1234
QDRANT_URL=http://localhost:6333

# News Ingestion Configuration
OLLAMA_BASE_URL=http://192.168.1.45:11434/api/chat
export LLM_MODEL=Qwen2.5-VL-7B-Instruct:latest
export LLM_PROVIDER=local
export LLM_TIMEOUT=120s

# Bootstrap Configuration (optional)
FSM_BOOTSTRAP_SEEDS=Science,Maths,Computing,Biology,Physics
FSM_BOOTSTRAP_MAX_DEPTH=1
FSM_BOOTSTRAP_MAX_NODES=50
FSM_BOOTSTRAP_RPM=60
FSM_BOOTSTRAP_SEED_BATCH=3
FSM_BOOTSTRAP_COOLDOWN_HOURS=3

# Executor Configuration - Conservative defaults to prevent GPU overload
# Reduce these if GPU is maxed out or timeouts occur
GOMAXPROCS=16
HDN_MAX_CONCURRENT_EXECUTIONS=3
MONITOR_MAX_CONCURRENT_GOALS=2
AUTO_EXECUTOR_MAX_CONCURRENT=2

# LLM Request Throttling - Limits concurrent LLM API calls to prevent GPU overload
# Default is 2 concurrent LLM requests. Increase only if GPU can handle more.
LLM_MAX_CONCURRENT_REQUESTS=2

# Docker resource limits optimized for x86
DOCKER_MEMORY_LIMIT=4g
DOCKER_CPU_LIMIT=8.0
DOCKER_PIDS_LIMIT=1024
DOCKER_TMPFS_SIZE=512m

# FSM Executor Configuration - Conservative defaults to prevent GPU overload
FSM_MAX_ACTIVE_GOALS=2
FSM_MAX_CONCURRENT_HYP_TESTS=1

# Additional performance tuning - Reduced to prevent GPU overload
FSM_BOOTSTRAP_RPM=30
FSM_BOOTSTRAP_SEED_BATCH=2
