se execute_code to fetch https://www.nationwide.co.uk/savings/compare-savings-accounts-and-isas/. The page contains a JSON structure with the key savingsProductList. Write a Python script to:

Fetch the page text.
Find the JSON data (it's inside the HTML).
JSON parse it to extract the savingsProductList.
Print a clean, formatted list of Product Name and AER for each account. Important: Do NOT print the raw HTML or the full JSON, only the extracted list."
